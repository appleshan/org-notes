录制宏

首先将光标移动到要处理的第一行。然后：
1. “C-x (” 开始录制宏；
2. “C-e” 光标跳至行尾；
3. 输入要插入的字符；
4. “C-n” 光标移动到下一行；
5. “C-x )” 结束录制宏。
接下来就可以通过执行宏对每一行都这么处理。
如果接下来只有 9 行，可以"C-u 9 C-x e"执行 9 次。
如果行数很多，可以选中要执行的那些行，然后"C-x C-k r"在选区里执行宏。

